# Legacy configurations

These files preserve historical experiments, fixed-power paper checks, and
the former split geometry/power inputs. They are not active canonical cases.

Active research entry points live only in `configs/cases/` and each load from
one case-level YAML. Literature fixed-power inputs retained here remain
reference/history data rather than analytical memory-power sources.
